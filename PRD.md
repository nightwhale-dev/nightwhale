# NightWhale 夜鲲 — Product Requirements Document

**版本**: v0.1 (MVP)
**日期**: 2026-08-21
**状态**: Draft for review

---

## 1. 一句话定位

NightWhale 不是插件市场，是一个 **idea 交易所**：把「别人遇到的问题 + 别人的解法」变成你能检索、试用、买入、拆解、改造的资产，而不是让你在几十个 dsh 插件仓库之间盲选。

## 2. 问题陈述

当前 dsh 插件生态的真实使用体验：

1. **发现成本高**：同一个问题（比如"如何让 agent 记住跨会话上下文"）可能有 5 个仓库都在解决，但你不知道它们存在，也没法快速比较。
2. **筛选依据缺失**：星标数、README 好看程度 ≠ 效果好。没人告诉你这个插件在你真正关心的场景下有没有用。
3. **同一问题，答案该是多元的**：insight 层面，"记忆管理"这个问题可以有 3 种正交的解法（摘要压缩 / 向量检索 / 结构化笔记）。市场需要的不是"最优解"排行榜，是让用户看到**全部候选**，凭自己的判断选，而不是被迫接受维护者的一种审美。
4. **改进的起点是拿到代码，不是拿到黑盒**：好的开发者不满足于"装上能用"，他们要看到实现，改一行 prompt、换一个检索策略，然后把改进的版本反哺回去。现在的插件安装方式（`npm install` 式的黑盒）阻断了这条路。

## 3. 核心 Insight（来自用户原话，不做转述稀释）

> 真正稀缺的不是代码——API token 极度丰富的时代，代码是被解放的生产力。真正稀缺的是 **不一样的想法**（不是附和大众共识的想法）。

由此推出的产品逆转：

- 传统插件市场的隐含假设：**存在一个客观最优插件，帮用户找到它**。
- NightWhale 的假设：**不存在唯一最优解，只存在"哪个 idea 匹配你当下的问题理解"**。同一个 problem 下，多个 idea 平等地陈列，用户的选择本身就是信号，而不是噪音要被排行榜压平。

## 4. 核心概念模型

```
Problem (问题)
 ├── 1..N 个 Idea (候选方案)
 │     ├── 描述：这个方案的思路是什么，为什么这样解决
 │     ├── 实现：绑定的代码 / dsh 插件 / prompt / skill 片段
 │     ├── 来源：作者、来源仓库链接（尊重原始出处）
 │     └── 信号：多少人 buy 过、多少人在 buy 之后保留 / 卸载
 └── 元数据：问题的抽象描述、适用场景、标签
```

- **Problem** 是一个可被搜索的意图单元。例："agent 跨 session 记不住用户偏好"。
- **Idea** 是绑定在 Problem 下的一份具体解法 + 代码引用。同一 Problem 下的 Idea 互相是**平级的候选**，没有默认排序，只有可见的采纳/淘汰信号。
- **Buy** 是把某个 Idea 的代码拉取到用户自己本地仓库的动作——不是付费购买，是"占用注意力/仓库空间"的隐喻，强调这是主动选择而非被动安装。
- **Uninstall = 淘汰信号**：用户卸载一个已 buy 的 idea 时，系统记录"这个 idea 在这个用户的场景下无效"，这个信号是公共的（匿名聚合），不是私有日志。

## 5. 核心组件（对应五个能力）

| 组件 | 职责 | MVP 范围 |
|---|---|---|
| **检索 Search** | 给定一个问题描述，找到匹配的 Problem，展示其下所有 Idea | 关键词 + 标签搜索，先不做语义向量检索 |
| **重组 Recompose** | 把一个 Idea 的代码从来源仓库中"拉出来"，落到用户本地仓库的规范位置 | `nightwhale buy <idea-id>` → clone/subtree 到本地目录 |
| **解构 Decompose** | 让用户能看到 Idea 背后完整的代码，而不是打包好的黑盒二进制 | 保证所有 Idea 必须链接可读源码（不收录纯二进制插件） |
| **个性化 Personalize** | 用户自己的仓库记录哪些 Idea 被 buy、被保留、被卸载、被怎么改 | 本地 `.nightwhale/ledger.json` 记录状态与个人修改 |
| **问题发现与方案选择** | 用户提出一个新 Problem（如果没人问过），或为已有 Problem 提交新 Idea | `nightwhale propose` 命令，落地为对公共仓库的 PR |

## 6. MVP 架构决策

### 6.1 个人仓库 = 本地目录 + CLI
- 用户机器上一个目录（`~/.nightwhale/`），CLI 工具操作它。
- 不做托管账户、不做后端登录。降低到"能跑"的最小面。
- 目录结构：
  ```
  ~/.nightwhale/
    ledger.json          # 记录 buy/uninstall 历史 + 状态
    registry/            # 公共 registry 的本地 clone（sync 命令维护）
    ideas/
      <idea-id>/         # 每个 buy 下来的 idea 的代码
        SOURCE.md        # 来源、作者、原始 problem 链接
        <实际代码文件>
  ```

### 6.2 公共 Idea 库 = GitHub repo 当数据库
- `nightwhale-dev/registry` 作为唯一的公共数据源。
- 结构：
  ```
  registry/
    problems/
      <problem-slug>/
        problem.yaml       # 标题、描述、标签
        ideas/
          <idea-slug>.yaml # 描述 + 代码来源链接（source_repo, source_path, author）
  ```
- **Propose 新 Idea = 提交 PR**：零后端成本，利用 GitHub 现成的评审、历史、协作机制。
- **Buy = CLI 读取 registry 的 yaml，拉取对应源码**（sparse checkout 只拉需要的文件）。
- **信号聚合**：MVP 阶段先记录在本地 `ledger.json`，匿名上报留到 v0.2。

### 6.3 明确排除在 MVP 外
- 不做插件评测集自动打分（多元化取代排行榜——不淘汰，只让选择可见）。
- 不做用户账户系统、不做付费。
- 不做语义搜索 / 向量库，关键词+标签够用来验证核心假设。
- 不做具体某个 dsh 插件该怎么写——那是"元框架验证第一步"，单独讨论。

## 7. 核心用户旅程（MVP）

1. **同步**：`nightwhale sync` → clone/pull 公共 registry 到本地。
2. **发现**：`nightwhale search "跨 session 记忆"` → 列出匹配的 Problem，每个 Problem 下列出 N 个 Idea。
3. **决策**：用户看候选 Idea 的简介和源码链接，自己判断。
4. **买入**：`nightwhale buy <idea-id>` → 代码落地本地，`SOURCE.md` 记录来源。
5. **使用与反馈**：在 dsh 里实际用这段代码。
6. **迭代**：
   - 好用 → 保留，`nightwhale improve <idea-id>` 记录修改。
   - 不好用 → `nightwhale uninstall <idea-id>`，标记 "tried, rejected"。
7. **贡献**：`nightwhale propose` 生成 PR 模板，提交到 registry。

## 8. 成功标准（MVP 验证目标）

验证一个假设：

> **"把同一问题的多个解法并列展示，比排行榜式的单一推荐，更符合开发者的真实决策方式。"**

MVP 验证方式：
- 手动往 registry 里放 3-5 个真实 Problem，每个配 2-3 个 Idea。
- 找 dsh 用户试用 `search` → `buy` 流程。
- 观察：他们是否真的会对比多个 Idea 再选。

## 9. 开放问题（下一轮讨论）

1. Idea 的"淘汰信号"要不要做成公共可见的聚合数据？怎么防刷/防恶意抹黑？
2. 一个 Idea 被大量"改进"之后，改进版本要不要变成新 Idea（fork 关系怎么表达）？
3. 具体的 dsh 插件应该怎么写、怎么被 NightWhale 索引——等个人 idea/问题发现结果。

---

*本 PRD 遵循原始讨论中的核心主张：产品的护城河是「让多元想法可见」，不是「帮用户做决定」。任何后续功能设计，如果结果是把多个 Idea 压缩成单一推荐，都违背这份 PRD 的第一原则。*
